/* @flow */
import React from 'react'
import { storiesOf } from '@storybook/react'

storiesOf('— Atoms', module).add('', () => (
  <p>
    Atoms are the smallest possible components, such as buttons, titles, inputs or event color
    pallets, animations, and fonts. They can be applied on any context, globally or within other
    components and templates, besides having many states, such as this example of button: disabled,
    hover, different sizes, etc.
  </p>
))
